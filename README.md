#Install sinatra using code: 
require 'sinatra'
get '/' do
'Hello World!'
end

You can run the site by compiling site.rb using the Ruby compiler.
i.e. ruby site.rb
It can then be viewed at your localhost. 
